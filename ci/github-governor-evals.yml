name: Governor Eval Structure

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate governor eval files
        run: ./scripts/governor-eval.sh --dry-run
